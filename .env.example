# Test-only fallback used by docker-compose.aws.yml when this variable is unset.
# Replace this before any deployment reachable outside a trusted test environment.
POSTGRES_PASSWORD=automark-test-only

# Optional host port for the web app (AWS Compose defaults to 80).
APP_PORT=8080
